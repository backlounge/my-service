# BackLounge 引き継ぎメモ（2026-07-30）

## 現在の販売状態

- 公開サイト: https://backlounge.net
- Cloudflare Pages の独自ドメイン接続・SSL: 有効
- 顧客管理システム（ライト版 19,800円／スタンダード版 39,800円）の購入申込導線: 稼働中
- 購入申込フォーム → D1保存 → 運営者通知メール → 申込者受付メール: 本番で到達確認済み
- Resend の `backlounge.net` ドメイン認証: verified

## 外部サービスの設定済み項目

- Cloudflare DNS: Pages用CNAME、Resend用DKIM・SPF（MX/TXT）・DMARCを登録済み
- Cloudflare Pages: `RESEND_API_KEY`、`CONTACT_NOTIFY_TO`、`CONTACT_FROM_EMAIL` をSecretとして設定済み
- Resend: 送信専用APIキーを作成済み

Secret、APIキー、個人情報はこのリポジトリ・この文書には記載しない。

## テスト記録

- 本番D1に「BackLounge 運営テスト」という購入申込が1件ある。
- 実際の注文ではないため、入金案内や納品の対象にしない。

## 次回の優先順(2026-07-31 追記: 下記1〜3は完了・本番反映済み)

- [x] `functions/_lib/site.js`、`public/robots.txt`、`public/sitemap.xml` 内の基準URLを `https://backlounge.net` に統一。
- [x] トップページ/商品一覧で顧客管理システムが「価格未定」と表示されていた不具合を修正(`functions/_lib/components.js` の `renderProductCard` を `editions[]` 対応に修正し、`products/[slug].js` と判定ロジックを統一)。コミット `bc8133c`。
- [x] GitHubへpush済み。local HEAD = origin/main HEAD = `bc8133c`。
- [x] Cloudflare Pages(Git連携)が自動ビルド・デプロイ済み。本番の `build-info.json` が `commitSha: bc8133c` であることを確認済み(2026-07-31)。
- [x] 本番で確認済み: トップページの価格表示、顧客管理/見積管理の商品ページ、申込フォーム(/contact)、各ページのcanonical、robots.txt、sitemap.xmlがすべて `backlounge.net` 基準で正しく表示。

残タスク:

1. 集客を継続する。対象は日本の個人サロン・美容室・整体・少人数店舗。宣伝投稿だけのコミュニティへの無差別参加はしない。

## 既存の運用資料

- `docs/販売運用.md`: 申込後の振込案内・入金確認・納品の手順と定型文
- `docs/入金確認台帳.csv`: 入金確認用の台帳

